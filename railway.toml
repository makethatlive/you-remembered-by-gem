[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx --yes prisma@5.22.0 generate && npm run build"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
